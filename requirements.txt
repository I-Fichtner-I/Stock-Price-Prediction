# Abhängigkeiten für notebooks/NVIDIA_Kursprognose_LSTM.ipynb
# Installation:  pip install -r notebooks/requirements.txt

# ── Kern (zwingend) ──────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.0.0
scipy>=1.11.0
matplotlib>=3.8.0
seaborn>=0.13.0
scikit-learn>=1.4.0        # MinMaxScaler, Metriken, MLP-Fallback
statsmodels>=0.14.0        # ADF-Test, ACF/PACF
yfinance>=0.2.40           # Kursdaten (mit synthetischem Fallback im Notebook)
tqdm>=4.60.0               # Fortschrittsbalken für ARIMA-Suche und LSTM-HPO

# ── Jupyter-Ausführung ───────────────────────────────────────────────────────
notebook>=7.0.0
nbconvert>=7.0.0
ipykernel>=6.0.0

# ── Optional: LSTM (empfohlen) ───────────────────────────────────────────────
# Ohne TensorFlow weicht das Notebook automatisch auf ein MLP-Ersatzmodell aus.
tensorflow-cpu>=2.16.0     # bzw. tensorflow>=2.16.0 (mit GPU)

# ── Optional: Explainability ─────────────────────────────────────────────────
# Ohne SHAP nutzt das Notebook eine Permutationswichtigkeit als Fallback.
shap>=0.45.0

# ── Optional: Interaktive Visualisierungen ────────────────────────────────────
# Ohne Plotly wird die interaktive Zelle übersprungen (statische Matplotlib-Charts
# aus Kap. 7 bleiben davon unberührt).
plotly>=5.20.0
